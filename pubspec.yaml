name: intel_comm_flutter
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.4 <3.0.0"

dependencies:
  http: ^0.13.5
  provider: ^6.0.5
  animated_text_kit: ^4.2.2
  google_fonts: ^4.0.3
  material_color_generator: ^1.1.0
  fl_chart: ^0.63.0

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
